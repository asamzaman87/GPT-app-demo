import React from 'react';
import { createRoot } from 'react-dom/client';
import RespondResult from './RespondResult';

createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <RespondResult />
  </React.StrictMode>
);

